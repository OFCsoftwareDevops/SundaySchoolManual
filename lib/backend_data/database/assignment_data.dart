// lib/backend_data/assignment_data.dart

import 'lesson_data.dart';

// We reuse EVERYTHING from lessons because the data is identical
typedef AssignmentDay = LessonDay;
typedef AssignmentSection = SectionNotes;